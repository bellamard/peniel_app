import React from 'react';
import { Text, View } from 'react-native';

const Predications = ({ navigation }) => (
    <View>
        <Text>Prédication</Text>
        <Text>En construction</Text>
    </View>
);

export default Predications;
